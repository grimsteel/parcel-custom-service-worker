{
  "name": "parcel-custom-service-worker-root",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "tests"
  ],
  "scripts": {
    "clean": "rm -rf .parcel-cache",
    "clean:all": "npm run clean --workspaces --include-workspace-root --if-present"
  }
}
